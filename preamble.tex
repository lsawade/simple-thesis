% -------------- Document layout settings ---------------------------------
% Let us set dimensions according to Princeton University thesis formatting guidelines
\setlength{\oddsidemargin}{.5in}
\setlength{\evensidemargin}{.5in}
\setlength{\topmargin}{-.5in}
\setlength{\textheight}{9in}
\setlength{\textwidth}{6in}
% -------------------------------------------------------------------------


% -------------- Fonts, spacing, etc. -------------------------------------
% Set double spacing
\usepackage{setspace}
\doublespacing
%

% -------------------------------------------------------------------------


% -------------- Macros for title etc. -----------------------------------

% Defining custom Macros such as \title,\author\date
% adviser
\makeatletter
\def\adviser#1{\gdef\@adviser{#1}}
\def\@adviser{\@latex@warning@no@line{No \noexpand\adviser given}}
\makeatother

% department macro
\makeatletter
\def\department#1{\gdef\@department{#1}}
\def\@department{\@latex@warning@no@line{No \noexpand\department given}}
\makeatother

% university macro
\makeatletter
\def\university#1{\gdef\@university{#1}}
\def\@university{\@latex@warning@no@line{No \noexpand\university given}}
\makeatother

% -------------------------------------------------------------------------


% -------------- Header ---------------------------------------------------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhead{}
\fancyhead[R]{\thepage}
\fancyhead[L]{\textit{\nouppercase{\rightmark}}}
\fancyfoot{}
% -------------------------------------------------------------------------


% -------------- Bibliography ---------------------------------------------

% And each chapter gets its own bibliography
\usepackage[T1]{fontenc}
\usepackage[latin1]{inputenc}
\usepackage{babel}
\usepackage[sectionbib]{chapterbib}



% Bibliography stuff
%\usepackage[round, sort, comma]{natbib}
%\bibliographystyle{plain}
\bibliographystyle{unsrt}
% -------------------------------------------------------------------------


% -------------- Figures, reference linking, captions, tables -------------
% This part sucks -> we have to add the graphics path for each chapter...
\usepackage{graphicx}
%\graphicspath{{chapters/chapter1/figures/}{chapters/chapter2/figures/}}

% Used for updating the graphics path in each chapter
% Note the line underneath the chapter init!! \graphicspath{{\currfiledir/figures/}}
\usepackage{currfile}

% We'd like to put each figure on its page
\usepackage{afterpage}

% Shall we use hyperref?
\usepackage{hyperref}

% Ya boi has some long captions
\usepackage{ccaption}% http://ctan.org/pkg/ccaption

% Tables
%\usepackage{tabularx}
%\usepackage{booktabs}
%\usepackage{multirow}
% -------------------------------------------------------------------------


% -------------- Random special packages ----------------------------------
% Also, some special packages
%\usepackage{gensymb}
%\usepackage{wasysym}


% New date formats
\usepackage{datetime}
\newdateformat{monthyeardate}{\monthname[\THEMONTH], \THEYEAR}
\newdateformat{yeardate}{\THEYEAR}

% Begin with some roman numerals
\pagenumbering{roman}

% Only needed for the template
\usepackage{lipsum}
% -------------------------------------------------------------------------





